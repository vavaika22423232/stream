# ===========================================
# YouTube Website Streamer - Environment Variables
# ===========================================
# Скопируйте этот файл в .env и заполните значения
# cp .env.example .env
# ===========================================

# ОБЯЗАТЕЛЬНО: Ключ трансляции YouTube
# Получить: YouTube Studio → Создать → Начать трансляцию → Ключ трансляции
STREAM_KEY=xxxx-xxxx-xxxx-xxxx-xxxx

# URL сайта для трансляции
TARGET_URL=https://neptun.in.ua

# YouTube RTMP сервер (обычно не нужно менять)
YOUTUBE_RTMP_URL=rtmp://a.rtmp.youtube.com/live2

# Разрешение видео
WIDTH=1920
HEIGHT=1080

# FPS (кадры в секунду)
FPS=30

# Битрейт видео (рекомендации YouTube: 1080p30 = 4500k, 720p30 = 2500k)
VIDEO_BITRATE=4500k

# Задержка перед перезапуском при ошибке (миллисекунды)
RESTART_DELAY=5000

# Интервал обновления страницы для предотвращения утечек памяти (миллисекунды)
# По умолчанию: 1 час (3600000)
PAGE_REFRESH_INTERVAL=3600000

# Включить отладочные логи (раскомментируйте для отладки)
# DEBUG=true
