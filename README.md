# YouTube Website Streamer

24/7 автоматическая трансляция сайта на YouTube Live через headless браузер.

## 🏗️ Архитектура

```
┌─────────────────┐     ┌──────────────┐     ┌─────────────┐     ┌──────────────┐
│   Puppeteer     │────▶│   FFmpeg     │────▶│    RTMP     │────▶│   YouTube    │
│   (Chromium)    │     │   Encoder    │     │   Stream    │     │    Live      │
│                 │     │              │     │             │     │              │
│ Headless режим  │     │ H.264 + AAC  │     │ flv format  │     │ Трансляция   │
│ 1920x1080       │     │ 4500kbps     │     │             │     │              │
└─────────────────┘     └──────────────┘     └─────────────┘     └──────────────┘
       │                       ▲
       │    JPEG frames        │
       └───────────────────────┘
            (через pipe)
```

### Как это работает:

1. **Puppeteer** запускает Chromium в headless режиме и открывает целевой сайт
2. **Цикл захвата** делает скриншоты страницы с заданным FPS (30 кадров/сек)
3. **FFmpeg** получает JPEG изображения через stdin и кодирует их в H.264 видео
4. Видеопоток отправляется на YouTube через **RTMP протокол**
5. **Автоперезапуск** при любых сбоях обеспечивает 24/7 работу

## 📋 Требования

- Node.js 18+
- FFmpeg
- Chromium/Chrome
- YouTube канал с включённой трансляцией

## 🚀 Быстрый старт (локально)

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

```bash
# Копируем пример
cp .env.example .env

# Редактируем .env и добавляем STREAM_KEY
```

### 3. Запуск

```bash
npm start
```

## 🐳 Docker

### Сборка образа

```bash
docker build -t youtube-streamer .
```

### Запуск контейнера

```bash
docker run -d \
  --name youtube-streamer \
  --restart=always \
  -e STREAM_KEY=your-youtube-stream-key \
  -e TARGET_URL=https://neptun.in.ua \
  youtube-streamer
```

## ☁️ Деплой на Render.com

### Способ 1: Через render.yaml (рекомендуется)

1. Загрузите проект в GitHub/GitLab репозиторий

2. Зайдите на [render.com](https://render.com) и создайте аккаунт

3. Нажмите **"New" → "Blueprint"**

4. Подключите ваш репозиторий

5. Render автоматически найдёт `render.yaml` и создаст сервис

6. **ВАЖНО:** После создания зайдите в настройки сервиса:
   - **Environment** → Добавьте `STREAM_KEY` (ваш ключ YouTube)

7. Нажмите **"Manual Deploy" → "Deploy latest commit"**

### Способ 2: Ручная настройка

1. Зайдите на [render.com](https://render.com)

2. Нажмите **"New" → "Background Worker"**

3. Подключите репозиторий

4. Настройки:
   - **Name:** `youtube-website-streamer`
   - **Region:** `Frankfurt` (или ближайший)
   - **Runtime:** `Docker`
   - **Plan:** `Standard` ($7/месяц) или выше

5. Environment Variables:
   ```
   STREAM_KEY=xxxx-xxxx-xxxx-xxxx-xxxx
   TARGET_URL=https://neptun.in.ua
   WIDTH=1920
   HEIGHT=1080
   FPS=30
   VIDEO_BITRATE=4500k
   ```

6. Нажмите **"Create Background Worker"**

## 🔑 Получение YouTube Stream Key

1. Зайдите на [YouTube Studio](https://studio.youtube.com)

2. Нажмите **"Создать" → "Начать трансляцию"**

3. Выберите **"Трансляция" (Stream)**

4. Скопируйте **"Ключ трансляции"** из настроек

5. **ВАЖНО:** Включите опцию "Включить автоматический запуск" в YouTube Studio

## ⚙️ Переменные окружения

| Переменная | Обязательно | По умолчанию | Описание |
|------------|-------------|--------------|----------|
| `STREAM_KEY` | ✅ | - | Ключ трансляции YouTube |
| `TARGET_URL` | ❌ | `https://neptun.in.ua` | URL сайта для трансляции |
| `WIDTH` | ❌ | `1920` | Ширина видео |
| `HEIGHT` | ❌ | `1080` | Высота видео |
| `FPS` | ❌ | `30` | Кадры в секунду |
| `VIDEO_BITRATE` | ❌ | `4500k` | Битрейт видео |
| `MUSIC_VOLUME` | ❌ | `0.3` | Громкость фоновой музыки (0.0-1.0) |
| `RESTART_DELAY` | ❌ | `5000` | Задержка перезапуска (мс) |
| `PAGE_REFRESH_INTERVAL` | ❌ | `3600000` | Интервал обновления страницы (мс) |
| `DEBUG` | ❌ | - | Включить отладочные логи |

## 🎵 Добавление фоновой музыки

Стример поддерживает фоновую музыку без авторских прав.

### Как добавить:

1. Скачайте royalty-free музыку с:
   - [Pixabay Music](https://pixabay.com/music/) (бесплатно)
   - [Free Music Archive](https://freemusicarchive.org/)
   - [YouTube Audio Library](https://studio.youtube.com/channel/UC/music)

2. Переименуйте файл в `background.mp3`

3. Положите в папку `music/`:
   ```
   stream/
   ├── music/
   │   └── background.mp3   ← сюда
   ├── src/
   └── ...
   ```

4. Закоммитьте и запушьте:
   ```bash
   git add music/background.mp3
   git commit -m "Add background music"
   git push
   ```

Музыка будет автоматически зациклена. Если файл не найден — будет тишина.

## 📊 Рекомендуемые настройки для YouTube

### Стандартное качество (1080p30)
```
WIDTH=1920
HEIGHT=1080
FPS=30
VIDEO_BITRATE=4500k
```

### Экономный режим (720p30)
```
WIDTH=1280
HEIGHT=720
FPS=30
VIDEO_BITRATE=2500k
```

### Высокое качество (1080p60)
```
WIDTH=1920
HEIGHT=1080
FPS=60
VIDEO_BITRATE=6000k
```

## 🔧 Устранение неполадок

### Стрим не запускается

1. Проверьте `STREAM_KEY` - он должен быть актуальным
2. Убедитесь что трансляция включена в YouTube Studio
3. Проверьте логи: `docker logs youtube-streamer`

### Низкий FPS / лаги

1. Уменьшите разрешение: `WIDTH=1280 HEIGHT=720`
2. Уменьшите FPS: `FPS=24`
3. Проверьте план на Render (нужен минимум Standard)

### Высокое потребление памяти

1. Уменьшите `PAGE_REFRESH_INTERVAL` для более частого обновления
2. Проверьте нет ли утечек на целевом сайте

### YouTube показывает "Нет данных"

1. Подождите 30-60 секунд после запуска
2. Включите "Автоматический запуск" в YouTube Studio
3. Проверьте что RTMP URL правильный

## 📝 Логи

Приложение выводит структурированные логи:

```
[2024-01-15T10:30:00.000Z] [INFO] Запуск YouTube Website Streamer
[2024-01-15T10:30:01.000Z] [INFO] Браузер запущен
[2024-01-15T10:30:05.000Z] [INFO] Страница загружена
[2024-01-15T10:30:07.000Z] [INFO] FFmpeg запущен
[2024-01-15T10:30:37.000Z] [INFO] Статистика: отправлено 900 кадров
```

## 💰 Стоимость на Render.com

| План | CPU | RAM | Цена | Рекомендация |
|------|-----|-----|------|--------------|
| Free | Shared | 512MB | $0 | ❌ Не подходит |
| Starter | Shared | 512MB | $7/мес | ⚠️ Минимум для тестов |
| Standard | 1 CPU | 2GB | $25/мес | ✅ Рекомендуется |
| Pro | 2 CPU | 4GB | $85/мес | ✅ Для высокого FPS |

## 📄 Лицензия

MIT

## ⚠️ Disclaimer

Убедитесь что у вас есть право транслировать контент целевого сайта. Автор не несёт ответственности за нарушение авторских прав.
